# OnlyOffice DocumentServer 运行时资产版本 —— 决定编辑器从哪个
# /public/v<版本>-<修订号>/ 目录加载 sdkjs / web-apps / 字体等静态资源。
#
# 必须与 scripts/fetch-assets.sh 拉取资产时使用的 DS_VERSION、HASH 参数保持一致：
#   bash scripts/fetch-assets.sh <DS_VERSION> <HASH>
# 若只用默认参数（DS_VERSION=9.3.1，HASH=1），保持下面的值不变即可；
# 换了参数拉取资产，也要同步把这里改成一致的 /v<版本>-<修订号>。
#
# 使用方法：复制本文件为 .env.local 后再运行 pnpm dev
# （.env.local 已被 .gitignore 忽略，不会被提交）。
NEXT_PUBLIC_APP_ROOT=/v9.3.1-1

# 站点对外基址 —— 用于 canonical 链接、sitemap.xml、robots.txt 及 Open Graph
# 元数据。本项目未内置固定域名（仅支持 Docker 自托管，不同部署者的实际访问
# 域名各不相同），构建时请设置为你的真实域名；不设置则兜底为本地开发地址。
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Google Analytics 属性 ID（可选）—— 完全留空是默认且推荐的隐私优先行为：
# 不设置此变量时，页面不会加载任何分析脚本，也不会向 Google 发出任何请求，
# 与本项目「数据不出浏览器」的核心主张一致。
# 只有当你作为自托管者自愿需要访问统计时，才在此处填入自己的 GA4 属性 ID
# （形如 G-XXXXXXXXXX）；填入后该实例会加载 Google Analytics 脚本。
# Docker 构建时也可用 ./build.sh 的第四个位置参数传入（见 build.sh 头部注释）。
NEXT_PUBLIC_GA_ID=
