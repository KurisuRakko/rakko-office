# OnlyOffice DocumentServer 运行时资产版本 —— 决定编辑器从哪个
# /public/v<版本>-<修订号>/ 目录加载 sdkjs / web-apps / 字体等静态资源。
#
# 必须与 scripts/fetch-assets.sh 拉取资产时使用的 DS_VERSION、HASH 参数保持一致：
#   bash scripts/fetch-assets.sh <DS_VERSION> <HASH>
# 若只用默认参数（DS_VERSION=9.3.1，HASH=1），保持下面的值不变即可；
# 换了参数拉取资产，也要同步把这里改成一致的 /v<版本>-<修订号>。
#
# 使用方法：复制本文件为 .env.local 后再运行 pnpm dev
# （.env.local 已被 .gitignore 忽略，不会被提交）。
NEXT_PUBLIC_APP_ROOT=/v9.3.1-1
